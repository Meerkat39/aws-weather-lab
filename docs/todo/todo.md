# todo

- [ ] 依存をインストールして開発サーバを起動する (`npm ci` → `npm run dev`) で表示確認
- [ ] `.env.local.example` を作成（`OPENWEATHER_API_KEY=your_api_key_here`）
- [ ] git 初期化と最初のコミット（`git init` / `git add .` / `git commit -m "chore: init"`）
- [ ] `src/lib/openWeather.ts` の雛形作成（API クライアントの関数シグネチャのみでも可）
- [ ] `src/app/(components)/WeatherCard.tsx` の雛形作成（表示コンポーネントの骨組み）
